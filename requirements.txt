torch>=1.12.0
numpy>=1.23
networkx>=2.6
matplotlib>=3.5
scikit-learn>=1.0
flask>=2.0
pytest>=7.0

# Optional: BugsInPy is useful for fetching labeled Python bugs but
# may not have wheels for the latest Python versions (e.g., 3.12).
# Install it only in a compatible environment (Python 3.8-3.11):
#   python -m pip install bugsinpy
# If you need it from source or a specific repo, install manually.

# Optional / dev extras
tqdm>=4.0
black>=23.0.0
